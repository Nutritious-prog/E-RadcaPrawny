import React, { ReactElement } from "react";
import { StyledActionsBar } from "./ActionsBar.style";

export const ActionsBar: React.FC = () => {
    return (
        <StyledActionsBar>aaa</StyledActionsBar>
    );
};
