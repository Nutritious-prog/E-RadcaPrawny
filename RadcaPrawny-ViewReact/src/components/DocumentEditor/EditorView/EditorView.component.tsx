import React from "react";
import { StyledEditorView } from "./EditorView.,style";

export const EditorView: React.FC = () => {
    return (
        <StyledEditorView>aaa</StyledEditorView>
    );
};
